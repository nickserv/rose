#!/usr/bin/env node
var mongoose = require('mongoose');
var seedData = require('../lib/seedData');
var seeds = require('../lib/seeds');

seeds(seedData).then(function () {
  mongoose.disconnect();
});
